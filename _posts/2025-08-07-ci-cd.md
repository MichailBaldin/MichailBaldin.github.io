---
layout: post
title: Настройка CI/CD для микросервисной архитектуры на GitHub Actions
date: 2025-08-04
tags: [go, микросервисы, cicd, github-actions, telegram, devops]
---

# Зачем CI/CD с самого начала?

После настройки базовой структуры проекта решил сразу настроить CI/CD.

**Преимущества раннего CI/CD:**

- Раннее выявление проблем интеграции
- Привычка делать небольшие, проверенные коммиты
- Мотивация — зеленые билды стимулируют продолжать

# Выбор платформы

Для микросервисного проекта на GitHub выбор был очевиден — **GitHub Actions**. Альтернативы типа Jenkins или GitLab CI требуют дополнительной инфраструктуры.

# Архитектура CI/CD

Для монорепозитория с тремя сервисами спроектировал следующую схему:

## Основной CI (`ci.yml`)

Запускается на `main` и `develop` ветках. Делает полную проверку:

- Go vet для всех модулей
- Форматирование кода
- Запуск тестов
- Сборка всех сервисов
- Golangci-lint проверки

## Feature CI (`feature.yml`)

Для `feature/*` веток. Более быстрая проверка — определяет изменившиеся сервисы и тестирует только их.

# Telegram уведомления

Решил настроить уведомления в Telegram, чтобы всегда быть в курсе статуса билдов, даже когда не за компьютером.

## Интеграция в workflow

Добавил уведомления в конце каждого job'а:

```yaml
- name: Notify Telegram on Success
  if: success()
  run: |
    curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
      -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
      -d parse_mode="HTML" \
      -d text="<b>BUILD SUCCESS</b>%0A<b>Repo:</b> ${{ github.repository }}%0A<b>Branch:</b> ${{ github.ref_name }}%0A<b>Author:</b> ${{ github.actor }}"

- name: Notify Telegram on Failure
  if: failure()
  run: |
    curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
      -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
      -d parse_mode="HTML" \
      -d text="<b>BUILD FAILED</b>%0A<b>Repo:</b> ${{ github.repository }}%0A<b>Branch:</b> ${{ github.ref_name }}%0A<b>Job:</b> ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
```

# Проблемы и их решение (реальные мучения!)

Конечно же, ничего не заработало с первого раза. Вот список всех проблем которые пришлось решать:

## Проблема #1: Go Workspace и команды из корня

Первый запуск CI завершился ошибкой:

```bash
pattern ./...: directory prefix . does not contain modules listed in go.work or their selected dependencies
```

**Причина:** команды `go vet ./...` и `go test ./...` не работают из корня проекта с Go workspace.

**Решение:** запускать команды из каждой папки сервиса отдельно:

```yaml
- name: Go vet for all modules
  run: |
    cd services/gateway && go vet ./...
    cd ../users && go vet ./...
    cd ../products && go vet ./...

- name: Run tests for all modules
  run: |
    cd services/gateway && go test -v ./...
    cd ../users && go test -v ./...
    cd ../products && go test -v ./...
```

## Проблема #2: Несоответствие версий Go

Вторая ошибка была коварнее:

**Причина:** В workflow файлах указал `GO_VERSION: '1.24.5'`, а в проекте везде `go 1.24`.

**Решение:** обновил версию Go в workflow файлах:

```yaml
env:
  GO_VERSION: "1.24" # было 1.24.5
```

## Проблема #3: go mod tidy изменяет go.work

После исправления версии получил новую ошибку:

```bash
Error: Process completed with exit code 1.
# git diff показывал изменения в go.work
```

**Причина:** команда `go work sync` переупорядочивает модули в файле go.work:

```diff
use (
	./services/gateway
-	./services/users
	./services/products
+	./services/users  # порядок изменился!
)
```

**Первоначальное решение:** пытался коммитить изменения автоматически, но это усложняло CI.

**Финальное решение:** убрал строгую проверку из workflow:

```yaml
# Удалил полностью этот проблемный блок
- name: Check if go mod tidy changed anything
  run: git diff --exit-code
```

Изменения в `go.work` — это нормально, Go workspace автоматически упорядочивает модули.

## Проблема #4: golangci-lint и Go workspace

Та же проблема повторилась с линтером:

```bash
level=error msg="[config_reader] The output format `github-actions` is deprecated, please use `colored-line-number`"
level=error msg="[linters_context] typechecking error: pattern ./..: directory prefix . does not contain modules listed in go.work or their selected dependencies"
Error: golangci-lint exit with code 7
```

**Причина:** `golangci-lint` также не понимает Go workspace из корневой директории.

**Решение:** запускать линтер для каждого модуля отдельно:

```yaml
- name: Run golangci-lint for gateway
  uses: golangci/golangci-lint-action@v3
  with:
    version: latest
    working-directory: services/gateway
    args: --timeout=5m

- name: Run golangci-lint for users
  uses: golangci/golangci-lint-action@v3
  with:
    version: latest
    working-directory: services/users
    args: --timeout=5m

- name: Run golangci-lint for products
  uses: golangci/golangci-lint-action@v3
  with:
    version: latest
    working-directory: services/products
    args: --timeout=5m
```

## Урок: Go workspace в CI — особенности

**Главный инсайт:** Go workspace отлично работает для локальной разработки, но создает проблемы в CI из-за того, что многие Go инструменты не умеют работать с workspace из корневой директории.

**Стратегия решения:** запускать все Go команды (`go vet`, `go test`, `go build`, `golangci-lint`) из папки каждого модуля отдельно, а не из корня проекта.

# Настройка Golangci-lint

Начал с полным конфигом линтера из документации, но получил десятки предупреждений на пустых проектах. Упростил до базового набора:

```yaml
linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - gofmt
    - goimports
```

Остальные линтеры добавлю по мере роста проекта — не стоит усложнять с самого начала.

# Локальное тестирование

Чтобы не ломать билды, создал Makefile для локальной эмуляции CI:

```makefile
ci-local:
	@echo "🔄 Running local CI checks..."
	go work sync
	cd services/gateway && go mod tidy
	cd services/users && go mod tidy
	cd services/products && go mod tidy
	cd services/gateway && go vet ./...
	cd services/users && go vet ./...
	cd services/products && go vet ./...
	# Проверка форматирования
	@UNFORMATTED=$$(find . -name "*.go" -not -path "./vendor/*" | xargs gofmt -s -l); \
	if [ -n "$$UNFORMATTED" ]; then \
		echo "The following files are not formatted:"; \
		echo "$$UNFORMATTED"; \
		exit 1; \
	fi
	cd services/gateway && go test -v ./...
	cd services/users && go test -v ./...
	cd services/products && go test -v ./...
	cd services/gateway && go build .
	cd services/users && go build .
	cd services/products && go build .
	@echo "All CI checks passed!"
```

Теперь перед каждым коммитом запускаю `make ci-local` — это экономит время и нервы. Лучше найти ошибку локально за 30 секунд, чем ждать 2 минуты пока упадет CI.

# Результат

После всех мучений получил рабочий CI/CD pipeline:

✅ **Автоматические проверки** для всех пуш в main/develop  
✅ **Быстрые проверки** для feature веток  
✅ **Telegram уведомления** с красивым форматированием  
✅ **Локальное тестирование** перед коммитом  
✅ **Линтер** для качества кода

# Структура файлов

Итоговая структура CI/CD файлов:

```
.github/
├── workflows/
│   ├── ci.yml          # Основной CI для main/develop
│   └── feature.yml     # Быстрый CI для feature веток
.golangci.yml           # Конфигурация линтера (упрощенная)
Makefile               # Локальное тестирование
```

# Следующие шаги

В планах добавить:

- **Docker build** — когда появятся Dockerfile'ы
- **Security scanning** — проверка зависимостей
- **Coverage reporting** — когда накопятся тесты
- **Automatic releases** — семантическое версионирование

Пока что сосредоточусь на разработке сервисов. Главная цель достигнута — есть автоматическая проверка качества кода и уведомления о проблемах.

# Выводы

Настройка CI/CD в начале проекта — одно из лучших решений, но **будьте готовы к проблемам с Go workspace**.

**Рекомендации:**

- Начинайте с простого CI — проверка компиляции и базовые линтеры
- Настройте уведомления сразу — будете всегда в курсе
- Создайте локальную эмуляцию CI — экономит время
- **Для Go workspace:** запускайте все команды из папок модулей, а не из корня
- Не переусложняйте с самого начала — добавляйте проверки по мере роста проекта

**Время потраченное:** около 4 часов на setup + debugging всех проблем.  
**Время сэкономленное в будущем:** бесценно!

В следующем посте начну разработку первого микросервиса — user-service с чистой архитектурой и минимальной функциональностью.
